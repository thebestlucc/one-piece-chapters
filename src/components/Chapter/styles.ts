import styled from 'styled-components';

export const Wrapper = styled.div`
  max-width: 980px;
  display: flex;
  justify-content: space-around;
  align-items: center;
  .cover {
    max-width: 720px;
  }
`;
